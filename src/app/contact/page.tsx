import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact Us | Aptech Ibadan",
  description:
    "Contact Aptech Ibadan for course enquiries, admissions guidance, campus visits, and career advice.",
};

export default function ContactPage() {
  return <ContactClient />;
}
